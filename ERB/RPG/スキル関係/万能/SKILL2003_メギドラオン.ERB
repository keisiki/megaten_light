;通常공격
;스킬のテンプレートになっています

;스킬名
@SKILL_NAME_2003,ARG
;アーベントが사용する場合P・ランチャーXに
;IF ARG > 0 && NO:ARG == [[キャラ:아벤트]]
IF SKILL_CHANGE_CHECK(ARG, [[キャラ:아벤트]]) == 1
	RESULTS = P・런처X
ELSE
	RESULTS = 메기도라온
ENDIF
;　명중
@SKILL_HITRATE_2003, ARG
RETURN 90

;　威힘
@SKILL_POWER_2003, ARG
;IF ARG > 0 && NO:ARG == [[キャラ:아벤트]]
IF SKILL_CHANGE_CHECK(ARG, [[キャラ:아벤트]]) == 1
	RETURN 350
ELSE
	RETURN 420
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2003,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2003,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2003, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2003,ARG
;IF ARG > 0 && NO:ARG == [[キャラ:아벤트]]
IF SKILL_CHANGE_CHECK(ARG, [[キャラ:아벤트]]) == 1
	RETURN 50
ELSE
	RETURN 60
ENDIF
;　相性
@SKILL_TYPE_2003,ARG
RETURN 17
;만능

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2003
RETURN 2
;魔法

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_2003
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2003
RETURN 1

;　사거리
@SKILL_RANGE_2003,ARG
RETURN 3

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_2003,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_2003
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2003,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2003
RETURN 20
;만능

;속도보정
@SKILL_SPEED_2003, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2003,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2003
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_2003
RETURN 15


;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2003,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2003,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2003,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2003,ARG
RETURN 0
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2003,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,2003






;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_2003
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:아벤트]]
	PRINTL [후열] 적 전체에 만능속성 대 마법데미지. 
	PRINTL 정식명칭은 「파르티잔・런처 X 모드」. 총신이 상하로 전개돼 발사가능해지는 강력한 빔 병기.
ELSE
	PRINTL [후열] 적 전체에 만능속성 특대 마법데미지.
ENDIF
;継承に필요な部位
@継承部位_2003,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2003,ARG
RETURN 1
;とくにない

