;通常공격
;스킬のテンプレートになっています

;스킬名
@SKILL_NAME_2002,ARG
;ファントムが사용する場合N・ブラスターに
IF ARG > 0 && NO:ARG == [[キャラ:팬텀]]
	RESULTS = N・블래스터
ELSE
	RESULTS = 메기도라
ENDIF

;　명중
@SKILL_HITRATE_2002, ARG
RETURN 90

;　위력
@SKILL_POWER_2002, ARG
IF ARG > 0 && NO:ARG == [[キャラ:팬텀]]
	RETURN 220
ELSE
	RETURN 295
ENDIF

;　최저공격횟수
@SKILL_MINATTACKNUMBER_2002,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_2002,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_2002, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_2002,ARG
IF ARG > 0 && NO:ARG == [[キャラ:팬텀]]
	RETURN 30
ELSE
	RETURN 35
ENDIF

;　相性
@SKILL_TYPE_2002,ARG
RETURN 17
;만능

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_2002
RETURN 2
;魔法

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_2002
RETURN 2


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_2002
RETURN 1

;　사거리
@SKILL_RANGE_2002,ARG
RETURN 3

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_2002,ARG
RETURN 3

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_2002
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_2002,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_2002
RETURN 20
;만능

;속도보정
@SKILL_SPEED_2002, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_2002,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_2002
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_2002
RETURN 12

@SKILL_RANKUP_2002
D:1 = 2003
;랭크アップ先は二つ
RETURN 1

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_2002,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_2002,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_2002,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_2002,ARG
RETURN 0
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_2002,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,2002






;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_2002
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:팬텀]]
	PRINTL [후열] 적 전체에 만능속성 중 마법데미지. 정식명칭은 「뉴트론・블래스터」
ELSE
	PRINTL [후열] 적 전체에 만능속성 대 마법데미지.
ENDIF

;継承に필요な部位
@継承部位_2002,ARG
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_2002,ARG
RETURN 1
;とくにない

