

;스킬名
@SKILL_NAME_22,ARG
;ばななが사용する場合小太刀・輪に
;IF ARG > 0 && NO:ARG == [[キャラ:나나]]
IF SKILL_CHANGE_CHECK(ARG, [[キャラ:나나]]) == 1
	RESULTS = 대태도・륜
ELSE
	RESULTS = 부월쇄파
ENDIF

;　명중
@SKILL_HITRATE_22, ARG
RETURN 100

;　威힘
@SKILL_POWER_22, ARG
RETURN 410

;　최저공격횟수
@SKILL_MINATTACKNUMBER_22,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_22,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_22, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_22,ARG
RETURN 32

;　상성
@SKILL_TYPE_22,ARG
RETURN 0
;검

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_22
RETURN 1
;特技

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_22
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_22
RETURN 1

;　사거리
@SKILL_RANGE_22,ARG
RETURN 2

;　範囲　11체　22列　3전체　4　적아군전체
@SKILL_SPHERE_22,ARG
RETURN 2

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_22
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_22,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_22
RETURN 0
;검격

;속도보정
@SKILL_SPEED_22, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_22,ARG

;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_22
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_22
RETURN 13

@SKILL_RANKUP_22
;랭크アップ先は二つ
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_22,ARG
RETURN GET_STATE_NUM("BIND")


;추가효과상성
@SKILL_ADDTIONAL_TYPE_22,ARG
RETURN GET_TYPE_NUM("신경")

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_22,ARG
RETURN 15

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_22,ARG
RETURN 95
;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_22,ARG,ARG:1
CALL ATTACK_FIELD,ARG,ARG:1,22






;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_22
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:나나]]
	PRINTL 소녀のキラめきにより鋳造された日本刀。
	PRINTL 大太刀を主体とした渾身の一撃により적前１列に大ダメージを与えBIND상태にする。後列사용可。
ELSE
	PRINTL [후열] 적 전열에 검격속성 특대 물리데미지. (낮은 확률로 BIND)
ENDIF

;継承に필요な部位
@継承部位_22,ARG
SIF TALENT:ARG:무기
	RETURN 1
RETURN 1

;사용조건
@SKILL_SPECIAL_ACTIONABLE_22,ARG
RETURN 1
;とくにない

