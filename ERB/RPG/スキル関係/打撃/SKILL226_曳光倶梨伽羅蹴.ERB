;通常공격
;스킬のテンプレートになっています

;스킬名
@SKILL_NAME_226,ARG
;まひるが사용する場合キラめきのありかに
;IF ARG > 0 && NO:ARG == [[キャラ:마히루]]
IF SKILL_CHANGE_CHECK(ARG, [[キャラ:마히루]]) == 1
	RESULTS = 반짝임이있는가
ELSE
	RESULTS = 예광구리가라축
ENDIF

;　명중
@SKILL_HITRATE_226, ARG
RETURN 95

;　威힘
@SKILL_POWER_226, ARG
SIF FLAG:拡散フラグ
	RETURN 420
RETURN 445

;　최저공격횟수
@SKILL_MINATTACKNUMBER_226,ARG
RETURN 1

;　최대공격횟수
@SKILL_MAXATTACKNUMBER_226,ARG
RETURN 1

;　ＨＰ2を支払うかＭＰ3を支払うか
@SKILL_COSTTYPE_226, ARG
RETURN 3
;소비なし＝1

;　コストの量
@SKILL_COST_226,ARG
RETURN 30

;　상성
@SKILL_TYPE_226,ARG
RETURN 2
;타격

;　1EXTRAか2MAGICか
@SKILL_DECIDE_TYPE_226
RETURN 1
;どちらでもない

;　1物理ダメージで計算するか2魔法ダメージで計算するか
@SKILL_DAMAGETYPE_226
RETURN 1


;　대상が1적か2아군か3場전체か4特殊か
@SKILL_TARGET_226
RETURN 1

;　사거리
@SKILL_RANGE_226,ARG
RETURN 2

;　範囲　11체　21열　3전체　4　적아군전체
@SKILL_SPHERE_226,ARG
RETURN 1

;　 効果の타입　1공격、2회복、3상태変化、4特殊、5상태회복
@SKILL_EFECT_226
RETURN 1

;　 特殊処理の有無、及び処理内容
@SKILL_SPECIALEFECT_226,ARG
;ARG = 1だった場合は処理の有無を返します
SIF ARG == 1
	RETURN 0
;以下に特殊な処理を記述します

;계승타입
@SKILL_SUCCESSION_TYPE_226
RETURN 2
;타격

;속도보정
@SKILL_SPEED_226, ARG = -1
RETURN 0

;　구상の표시とその타입
@SKILL_MESSAGE_226,ARG


;전투時以外に사용できるか
@SKILL_ACTIONABLE_FIELD_226
RETURN 0

;스킬変化？用랭크
@SKILL_RANK_226
RETURN 14

@SKILL_RANKUP_226
RETURN 0

;テンプレート用、추가효과シリーズ
;추가효과스테이터스
@SKILL_ADDTIONAL_STATE_226,ARG
RETURN 0

;추가효과상성
@SKILL_ADDTIONAL_TYPE_226,ARG
RETURN 0

;추가효과기본부여확률
@SKILL_ADDTIONAL_BASE_RATE_226,ARG
RETURN 0

;추가효과부여확률上限
@SKILL_ADDTIONAL_MAX_RATE_226,ARG
RETURN 0

;拡散공격用추가処理
@SKILL_SPREAD_226,ARG
RETURN 1
;拡散距離:1

;====================================================
;스킬及び行動の実行
;====================================================
@ACTION_226,ARG,ARG:1
CALL ATTACK_SPREAD,ARG,ARG:1,226



;====================================================
;実行内容ここまで
;====================================================

;　 説明文の표시
@SKILL_EXPLAIN_226
IF U > 0 && U < CHARANUM && NO:U == [[キャラ:마히루]]
	PRINTL 소녀のキラめきが、舞台装置を稼働させ、いつかのレヴューを再現する。
	PRINTL 具体的には손あたり次第メイスでホームランする。
	PRINTL 대상とその周囲をレヴューに引き込み、타격相性の特大ダメージ。後列사용可。
ELSE
	PRINTL [후열] 전열의 적 1체와 주변의 적에게 타격속성 특대 물리데미지.
ENDIF

;継承に필요な部位
@継承部位_226,ARG
RETURN 1


;사용조건
@SKILL_SPECIAL_ACTIONABLE_226,ARG
RETURN 1
;とくにない




