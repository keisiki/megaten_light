
;의뢰フラグ:80:0を参照する部分
;32個(CASE 31)まで作成可能
@REQUEST_80_PLAYABLE_0(ARG)
SELECTCASE ARG
CASE 0
	;조건はダークかノーマル
	SIF (ABL:MASTER:속성LD == 3 || ABL:MASTER:속성LD == 2)
		RETURN 1
CASE 1 
	;조건は貢献度20万以上で、ライトかノーマル
	IF FLAG:組織貢献度 >= 200000
		SIF (ABL:MASTER:속성LD == 1 || ABL:MASTER:속성LD == 2)
			RETURN 1
	ENDIF
CASE 2
	
CASE 3
	
ENDSELECT
RETURN 0


;의뢰フラグ:80:1を参照する部分(現在は未사용なのでコメントアウト)
;以降は末꼬리の数字を増やしていく事で対応可能
;@REQUEST_80_PLAYABLE_1(ARG)
;SELECTCASE ARG
;ENDSELECT
;RETURN 0
