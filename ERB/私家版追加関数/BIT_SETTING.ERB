;=================================================================
;그외(전투関連以外)설정スイッチに関する関数郡
; FLAG:그외설정スイッチ にBIT値で설정を記録する
;  0 = 外見・スリーサイズ기능설정
;  1 = 마카レート変動기능설정

;=================================================================

;-----------------------------------------------------------------
;@SETTING_IS_3SIZE
;外見・スリーサイズ기능を이용するか설정を返す
;　引数　なし
; 戻り値 0=설정が무효 1=설정が有効
;-----------------------------------------------------------------
@SETTING_IS_3SIZE
#FUNCTION
#LOCALSIZE  1
#LOCALSSIZE 1
	;설정が有効
	SIF GETBIT(FLAG:그외설정スイッチ, 0) == 1
		RETURNF 1
	;설정が무효
	RETURNF 0

;-----------------------------------------------------------------
;@SETTING_INVERT_3SIZE
;外見・スリーサイズ기능を이용するかの설정を行う、関数を呼び出すごとにON/OFF변경
;　引数　なし
; 戻り値 なし
;-----------------------------------------------------------------
@SETTING_INVERT_3SIZE
#LOCALSIZE  1
#LOCALSSIZE 1
	INVERTBIT FLAG:그외설정スイッチ, 0


;-----------------------------------------------------------------
;@SETTING_SET_3SIZE
;外見・スリーサイズ기능を이용するかの설정を行う
;　引数　ARG:0 0=설정が무효 1=설정が有効
; 戻り値 なし
;-----------------------------------------------------------------
@SETTING_SET_3SIZE, ARG
#LOCALSIZE  1
#LOCALSSIZE 1
	IF ARG == 0
		CLEARBIT FLAG:그외설정スイッチ, 0
	ELSE
		SETBIT FLAG:그외설정スイッチ, 0
	ENDIF

;-----------------------------------------------------------------
;@SETTING_IS_MAKKA_RATE
;마카レート変動기능を이용するか설정を返す
;　引数　なし
; 戻り値 0=설정が무효 1=설정が有効
;-----------------------------------------------------------------
@SETTING_IS_MAKKA_RATE
#FUNCTION
#LOCALSIZE  1
#LOCALSSIZE 1
	;설정が有効
	SIF GETBIT(FLAG:그외설정スイッチ, 1) == 1
		RETURNF 1
	;설정が무효
	RETURNF 0

;-----------------------------------------------------------------
;@SETTING_INVERT_MAKKA_RATE
;마카レート変動기능を이용するかの설정を行う、関数を呼び出すごとにON/OFF변경
;　引数　なし
; 戻り値 なし
;-----------------------------------------------------------------
@SETTING_INVERT_MAKKA_RATE
#LOCALSIZE  1
#LOCALSSIZE 1
	INVERTBIT FLAG:그외설정スイッチ, 1


;-----------------------------------------------------------------
;@SETTING_SET_MAKKA_RATE
;마카レート変動기능を이용するかの설정を行う
;　引数　ARG:0 0=설정が무효 1=설정が有効
; 戻り値 なし
;-----------------------------------------------------------------
@SETTING_SET_MAKKA_RATE, ARG
#LOCALSIZE  1
#LOCALSSIZE 1
	IF ARG == 0
		CLEARBIT FLAG:그외설정スイッチ, 1
	ELSE
		SETBIT FLAG:그외설정スイッチ, 1
	ENDIF
